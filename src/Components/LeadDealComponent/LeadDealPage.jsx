import { Box } from "@mui/material";
import NavBar from "../NavBarComponent/NavBar";

const LeadDealPage = (props) => {
  return (
    <Box>
      <NavBar pageName="LeadDealPage" />
      <h1>Content</h1>
    </Box>
  );
};

export default LeadDealPage;
